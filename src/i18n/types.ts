export type TranslationKeys = {
    streamAlert: {
        title: string;
        game: string;
        topic: string;
        onAir: string;
        viewers: string;
        join: string;
        watchOnTwitch: string;
        supportTitle: string;
        donate: string;
        subscribe: string;
        chat: string;
    };
};

export type Locale = 'en' | 'uk';